# action.yml
name: 'download file'
description: 'download file to qbee file manager'
inputs:
  token:
    description: 'jwt token'
    required: true
outputs:
  token:
    description: 'jwt token'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.token }}